# FastAPI Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Core Dependencies
requests==2.31.0
python-dotenv==1.0.0
aiohttp==3.9.1  # Also used for Google Geocoding/Places API calls

# Exa Search API (Exa 2.0 with fast/deep search types and summary schemas)
exa-py>=1.8.0

# Caching
redis[hiredis]==5.0.1

# Data Models
pydantic>=2.0.0

# AI-Powered Scraping (Optional - for direct price extraction)
# Fast HTTP mode - no browser needed!
beautifulsoup4>=4.12.0  # Fast HTML parsing
lxml>=4.9.0  # Fast XML/HTML parser (optional but recommended)
openai>=1.0.0  # Optional - for GPT-based extraction
anthropic>=0.18.0  # Optional - for Claude-based extraction

# Partner API Clients (Optional - for official grocery store APIs)
pycryptodome>=3.19.0  # For Walmart API RSA signatures

# Fast JavaScript Scraping (for JS-rendered sites like Costco, Target, Walmart)
playwright>=1.40.0  # Headless browser automation - run: playwright install chromium
